@model IEnumerable<AfterSki.Models.RideModels.RideStatistic>
@{
    ViewData["Title"] = "Skidata";
}
<h2>@ViewData["Title"].</h2>
<h3>@ViewData["Message"]</h3>
@*
<table class="table">
    <tr>
        <th class="tablewidth-250">
            @Html.DisplayNameFor(model => )
        </th>
        <th class="tablewidth-150">
            @Html.DisplayNameFor(model => model.Title)
            @Html.DropDownList("searchTitle", "All")
        </th>
        <th class="tablewidth-350">
            @Html.DisplayNameFor(model => model.WorkType)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.StartDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.EndDate)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
            {
        <tr>
            <td class="tablewidth-250">
                <span align="left" class="glyphicon glyphicon-briefcase glyphicon-35px"></span> 
                <strong>@Html.DisplayFor(modelItem => item.destination.name)</strong>
            </td>
            <td class="tablewidth-100">
                @Html.DisplayFor(modelItem => item.liftName)
            </td>
            <td class="tablewidth-350">
                @Html.DisplayFor(modelItem => item.height)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.swipeDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.swipeTime)
            </td>

            <td>
                <a asp-action="Edit" asp-route-id="@item.ID">Ändra</a> |
                <a asp-action="Details" asp-route-id="@item.ID">Se detaljer</a> |
                <a asp-action="Delete" asp-route-id="@item.ID">Ta bort</a>
            </td>
        </tr>
    }
</table>*@
